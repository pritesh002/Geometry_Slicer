{
  "name": "webgl-mesh-cutter",
  "private": true,
  "scripts": {
    "dev": "vite --open Canvas.html",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "gl-matrix": "^3.4.4"
  }
}
